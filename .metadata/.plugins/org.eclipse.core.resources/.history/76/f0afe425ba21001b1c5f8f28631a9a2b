import com.phone.*;

public class FactoryMain {
   public static void main(String [] args) {
	   //OS o = new Android();
	   OS o = new Windows();
	   // Supoose in future Windows beomes good and its famous --> then we need to change the implementation of the Windows class--> Recompile it
	   //Which is not good way --> Windows Gives Implementaion heres itself
	   o.spec();
   }
}
